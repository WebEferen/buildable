{
  "name": "monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "buildable run \"npm run build\"",
    "start": "buildable run \"npm run start\"",
    "dependency-graph": "buildable dependency-graph"
  },
  "devDependencies": {
    "@webeferen/buildable": "^0.6.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
